<p style="color: green"><%= notice %></p>

<% content_for :title, "Questions" %>

<div class="container mx-auto px-4 py-8 mt-16">
  <div class="text-center mb-8">
    <h1 class="text-3xl font-bold text-base-content">Community Questions</h1>
    <p class="text-base-content/75 mt-2">Browse questions submitted by our community</p>
  </div>

  <div class="max-w-5xl mx-auto">
    <div class="flex flex-col md:flex-row justify-between gap-4 mb-8">
      <div class="md:w-2/3">
        <div class="bg-base-200 rounded-lg shadow-md p-4">
          <input type="text" placeholder="Search questions..." class="w-full px-4 py-2 rounded-md bg-base-100 text-base-content border border-base-300 focus:outline-none focus:ring-2 focus:ring-accent">
        </div>
      </div>
      <div class="md:w-1/3 flex gap-2">
        <select class="flex-grow px-4 py-2 rounded-md bg-base-200 text-base-content border border-base-300 focus:outline-none focus:ring-2 focus:ring-accent">
          <option value="">All Statuses</option>
          <option value="pending">Pending</option>
          <option value="in_progress">In Progress</option>
          <option value="answered">Answered</option>
        </select>
        <%= link_to "Ask a Question", new_question_path, class: "btn btn-accent" %>
      </div>
    </div>

    <div class="space-y-6" id="questions">
      <% @questions.each do |question| %>
      <div class="bg-base-100 rounded-lg shadow-md overflow-hidden">
        <div class="p-6">
          <div class="flex justify-between items-start">
            <div class="w-full">
              <div class="flex justify-between items-center mb-2">
                <h2 class="text-xl font-bold text-base-content"><%= question.title %></h2>
                <% case question.status %>
                <% when "pending" %>
                <span class="px-3 py-1 bg-warning/20 text-warning rounded-full text-sm font-medium">Pending</span>
                <% when "in_progress" %>
                <span class="px-3 py-1 bg-info/20 text-info rounded-full text-sm font-medium">In Progress</span>
                <% when "answered" %>
                <span class="px-3 py-1 bg-success/20 text-success rounded-full text-sm font-medium">Answered</span>
                <% else %>
                <span class="px-3 py-1 bg-base-200 text-base-content/80 rounded-full text-sm font-medium">Unknown</span>
                <% end %>
              </div>
              <p class="text-base-content/80 mt-2"><%= truncate(question.body, length: 200) %></p>
              <div class="mt-4 flex flex-wrap items-center justify-between">
                <div class="text-sm text-base-content/60">
                  <% if question.anonymous? %>
                  <span>Asked by: Anonymous</span>
                  <% else %>
                  <span>Asked by: <%= question.email.split('@').first %></span>
                  <% end %>
                  <span class="mx-2">•</span>
                  <span><%= question.created_at.strftime("%B %d, %Y") %></span>
                </div>
                <div class="mt-2 sm:mt-0">
                  <%= link_to "View question", question_path(question), class: "text-accent hover:text-accent-focus transition-colors" %>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
      <% end %>

      <% if @questions.empty? %>
      <div class="text-center py-12 bg-base-100 rounded-lg shadow-md">
        <div class="text-4xl text-base-content/30 mb-4">❓</div>
        <h3 class="text-xl text-base-content font-medium">No questions yet</h3>
        <p class="text-base-content/70 mt-2">Be the first to ask a question</p>
      </div>
      <% end %>
    </div>

    <div class="mt-8 flex justify-center">
      <div class="join">
        <button class="join-item btn btn-sm">«</button>
        <button class="join-item btn btn-sm btn-active">1</button>
        <button class="join-item btn btn-sm">2</button>
        <button class="join-item btn btn-sm">3</button>
        <button class="join-item btn btn-sm">»</button>
      </div>
    </div>
  </div>
</div>
